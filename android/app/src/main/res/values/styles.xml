<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while el proceso está iniciando cuando el modo oscuro está apagado -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Mostrar una pantalla de presentación en la actividad. Se elimina automáticamente cuando
             Flutter dibuja su primer frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <!-- Theme aplicado a la ventana de Android una vez que el proceso ha iniciado.
         Determina el color de la ventana mientras Flutter se inicializa y detrás de la UI de Flutter mientras se ejecuta. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>

        <!-- 🔹 Deshabilitar el Swipe-to-Dismiss en Wear OS -->
        <item name="android:windowSwipeToDismiss">false</item>
    </style>
</resources>
